# v5 原型验证记录

日期：2026-09-08。范围为本工作树的本地原型；浏览器交互使用 Chrome 与 CUA，没有运行真实生成、收费操作或生产 CRM 请求。

## 已实测的交互

| 场景             | 证据与结果                                                                                                                                   |
| ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| 画布与资产入画布 | 初始 5 节点 / 2 连线；点击素材 + 后 6 / 2；撤销回到 5 / 2                                                                                    |
| 参考连接         | 点击输出及输入端口后 5 节点 / 3 连线；撤销恢复 2 连线                                                                                        |
| 节点拖动         | 图片节点从 `(42,322)` 移到 `(87,352)`，实际 DOM 坐标变化；提供键盘替代                                                                       |
| 文字创建/编辑    | 新建文字，清空标题出现“请填写节点标题”；填写标题、正文后显示已保存节点；节点移除可撤销                                                       |
| 策划最大化       | 入口位于节点头部；URL 为 `node=…&mode=maximized&tab=shots`；顶部/工具栏无策划独立入口                                                        |
| 还原视口         | 展开前后 `#world` transform 完全一致；原节点仍保留并选中                                                                                     |
| 资产类型筛选     | 文字类型得到 3 份素材，切回全部恢复结果                                                                                                      |
| 标签实时反馈     | 选中“海边”立即得到 5 份素材，选择面板同步显示“实时匹配 5 份素材”                                                                             |
| 市场示例         | 搜索“布光”得到 2 条；收藏免费示例后显示“已收藏”，个人库增加该素材                                                                            |
| 软删除与恢复     | 将本轮收藏的示例移入回收站，显示剩余 30 天；恢复后回收站为 0，个人库保留素材；未测试真实数据的永久清除                                       |
| 集合             | 3 个普通集合 / 2 个固定入口；普通集合有打开、重命名、删除菜单；固定集合不展示管理菜单                                                        |
| Skill 输入/产出  | 选择演示订单，生成可编辑示例策划；产物明确未发布，并在 Agent 中提供结果入口                                                                  |
| 分镜编辑         | 在最大化视图中选择替换参考图、修改指导并保存，分镜显示新图片和文字                                                                           |
| 分镜移除入口     | 打开具名确认并取消，明确保留资产与已发布/现场快照；删除模型边界由 check.mjs 验证                                                             |
| 发布与跳转       | 默认发布目标为本次来源 `demo-sea`；发布 v2 后跳转到虚构订单详情，显示所关联的 v2 策划                                                        |
| 现场与版本       | 标记 1 / 3 镜头、前进到第 2 镜；编辑工作稿至 v3 后，现场仍使用 v2；刷新后保留进度                                                            |
| 文档独立         | 移除策划摘要节点后，“引用已有策划”仍列出相同文档及 v3 / 3 分镜；放回画布成功                                                                 |
| 窄屏             | 821px 最大化面板宽 723px；375px 最大化面板宽 297px、分镜纵排，后期章节可访问；页面无横向溢出                                                 |
| 窄屏资产弹窗     | 修复旧导入弹窗 ID 固定宽度后，375px 下弹窗实际宽 274px；可打开、关闭并继续进入集合                                                           |
| 存储保护         | 走查中遇到修订冲突时未覆盖数据，显示导出/刷新提示；刷新读取最新版本后可继续。随后视口记忆改为标签页 sessionStorage，避免视口动作争用内容修订 |

## 视觉与计算值

桌面实际查看画布、双列素材侧栏、类型菜单、标签下拉、市场和最大化策划截图。界面计算值：背景 `#141619`、正文 `#eeeee9`、次要文字 `#a8adb5`，玻璃为 `blur(28px) saturate(1.22)`，滚动条 thumb 为 `rgb(85,93,102)`。

窄屏尺寸通过 Chrome viewport 能力与 DOM 几何/交互核对。该能力的截图显示存在额外缩放，因此窄屏截图仅辅助观察，不作为逐像素对照证据。结束时恢复浏览器原尺寸。

## 命令验证

- `node check.mjs`：通过文档独立、发布/现场快照、参考边守卫、侧栏几何兼容、最大化入口与资源清单检查。
- v4 `check.mjs`、`recycle.check.mjs`、`tags.check.mjs`：通过。新侧栏变体未改变 v4 默认的几何与领域行为。
- v5 ES module 的 `node --check`：通过。
- Prettier 3.6.2 格式检查：通过；`git diff --check`：通过。
- `designmd lint DESIGN.md`：0 errors / 0 warnings。
- Premium strict 静态审计退出 1，保留原始结果 [premium-audit.json](evidence/premium-audit.json)：42 个按钮被报为未识别行为，1 个 textarea 被报为未识别 resize。逐项源码核对发现 38 个 ID 按钮在外置模块绑定，4 个市场类型按钮由 library.mjs 的父容器委托处理；textarea 在 canvas.css 的全局规则中明确 `resize: none`，浏览器实测一致。本轮不宣称 strict 审计通过，也不为消除扫描误报改成内联事件或重复样式。

## 未覆盖的范围

没有真实模型/RAG/3D 渲染、市场交易、生产授权、云同步、设备间离线一致性或性能容量验证；音视频播放器依赖浏览器对所选文件的编码支持。没有把浏览器交互原型的通过结果当成生产服务验收。

## 2026-09-08 交互修订复核

- Chrome 实测左键框选：选中目标节点，world transform 保持不变。
- 节点拖动 40px/40px，工具条同步位移 40px/40px，距节点顶部保持约 12px；测试拖动已通过撤销还原。
- Shift 点击追加 / 再次点击取消：节点选择和 Agent 引用数量同步为 2 / 1。
- 手形工具平移 50px/40px，world transform 同步变化，保留引用。中键与空格进入同一平移分支，未完成硬件中键和持续按空格的自动化实测。
- LLM 自定义菜单正常展开，选择 Gemini 后标签实时更新；仍为明确标注的演示。
- 图片选择器已触发；Chrome 自动化扩展拒绝 setFiles（Not allowed），因此本轮未完成真实文件附加与发送的浏览器端到端验证。上传校验复用已有 readImage，附件状态经源码检查；不把该限制报告为测试通过。
- 通过 v5 check.mjs、修改模块语法检查与格式检查。仅修改独立原型及说明。

### 媒体预览修订

Chrome 实测图片加载成功（850px 原图），普通节点光标为 auto；Esc、关闭按钮和外部区域点击均关闭预览，媒体 DOM 清空、选择保留、焦点回到工具条按钮。文字节点不显示放大入口。视频控件与暂停清理路径完成源码检查，本轮未使用实际视频文件完成播放实测。v5 契约检查与语法检查通过。

### 简洁预览与下载（2026-09-08）

Chrome 实测预览没有可见标题或提示文字，图片正常加载，关闭按钮为 40px。下载点击后恢复可用并显示已发起提示；自动化 download 事件超时，浏览器策略禁止打开下载记录页，因此未确认最终落盘结果，不将其标记为端到端通过。下载源码包含 Blob 获取、格式后缀、临时链接清理及失败恢复。契约检查、语法与格式检查通过。

### 加号连线交互（2026-09-09）

- Chrome 验证点击右侧加号打开五种通用节点类型菜单，选择图片后由 7 节点 / 2 连线变为 8 / 3，自动连接方向正确。
- 拖动右侧加号到 (1460,560)，菜单实际 left/top 为 (1460,560)，创建视频后自动连接；左侧拖动创建图片验证为“新的图片 → 海岸的色彩与呼吸”。
- 拖到已有文字节点创建参考边，节点数量保持 7；测试创建与连线均已通过撤销恢复为原来的 7 / 2。
- 菜单取消与创建完成后的临时 path 数量为 0。验证中发现浏览器缓存旧版 ui.mjs 未调用新增菜单清理钩子；所有导入统一更新同一模块 URL，避免双份 UI 单例，并复测取消成功。
- 取消选择并移到空白处后端口 pointer-events 为 none、淡出完成 opacity 为 0。未更改原有素材和策划文档。

### 邻近吸附与锚点修正（2026-09-09）

Chrome 实测未选中节点时，在加号静止中心约 32px 外点击空白，near-pointer 生效且产生约 9px 屏幕位移。点击吸附后的加号，临时 SVG 起点与按钮实际中心 x/y 差均为 0px，默认终点 y 差为 0px。拖拽到 (1470,560) 后，SVG 终点还原到屏幕坐标为 (1470,560)，起点误差仍为 0px。取消菜单后退出预览；未创建测试节点。定位并修正默认终点计算漏掉节点 y 坐标的问题。契约检查、语法检查通过。

### 连线与加号解耦纠正（2026-09-09）

最新要求取代上次“锚定加号中心”的验证目标。Chrome 实测加号吸附位移非零时，两条已有连线的 path 数据与位移前完全一致；点击加号后临时线起点与节点右边缘中心的 x/y 误差均为 0px。吸附模块不再触发连线重绘。契约及语法检查通过。

### 选区连线高亮与流光

Chrome 实测选中海岸图片仅激活其一条出边，另一条无关边保持原样；选中两条入边的目标图片，两条边均高亮。动画计算周期 2.8s，dashoffset 随时间减小，方向沿 source → target；流光 pointer-events 为 none。空白点击后关联高亮数量回到 0。契约及语法检查通过。

## 2026-09-09 · 原生画布资产侧栏

本节替代此前“v4 iframe 侧栏”的运行时说明，历史记录保留。

- Chrome 实测旧数据 17 份全部保留，页面 iframe 数为 0。首次复制到独立原生库，避免旧页定时写入导致 revision 冲突；原数据库保留。
- 光的形状分组 6 份；黑白标签 2 份；追加布光后交集 0；改任一条件为 4。查询建议显示标签/分组身份，条件芯片可移除。
- 多选 2 份入画布：7→9 节点；一次撤销回 7。单份拖放到 (1630,700)，生成节点 DOM 左上角实测为 (1630,700)，随后撤销。
- 创建测试分组与文字，默认归入当前组；就地新建标签后显示“1 已选”。刷新后资产 18 份，按新标签检索得 1 份。
- 测试素材移入回收站/恢复后原分组恢复；删除测试分组后素材仍保留并进入未归类。节点反向存入库成功。测试素材、分组和标签均经界面清理。最终保留原有 17 资产、7 节点、2 连线。
- 展开侧栏宽 718px 内容区 scrollWidth=clientWidth=519px；821px 窄屏 scrollWidth=clientWidth=305px。375px 修正后侧栏 left=62、right=363，scrollWidth=clientWidth=288px；没有横向溢出。临时视口已恢复。
- 日志检查发现画布 ResizeObserver 可能早于文档加载执行，补充文档就绪判断。
- `asset-store.check.mjs` 验证分组循环拒绝、同层排序、多重归类、未归类判定、恢复归类与稳定标签 ID。原型 `check.mjs` 与模块语法检查通过。
- 文件选择器在此前会话被浏览器扩展权限阻止，本轮未绕过权限，也不宣称本地媒体文件上传或实际下载完成经过端到端验证。文件导入与失败重试已实现；文字创建、检索及本地状态保存路径已实测；链接字段已实现按类型校验，尚未单独端到端验证。市场仍为示例，未连接真实交易或 RAG。

## 2026-09-09 · 画布节点打组

- 本轮开始画布已有 7 个节点、3 条连线，以本轮读取的页面基线为准，保留新增的第三条连线。
- Chrome 左键框选 n-brief/n-sea 后打组，所有原节点 DOM 世界位置与三条 SVG 路径完全不变。
- 组标题从 (565,393) 拖至 (645,303)，两个子节点同向移动约 (80.16,-89.74) CSS px（世界坐标取整）；组外节点位置不变。工具栏位于组上方，相关两条边高亮并跟随，第三条边不变。
- ⌘/Ctrl Shift G 解组后子节点 DOM 位置与连线保持一致；撤销恢复组。单独拖 n-sea 后 n-brief 位置不变。
- 撤销位移后刷新：仍为 7 个内容节点、1 个组、3 条连线，原位置与路径不变。解组清理。
- ⌘/Ctrl G 成组成功；Shift 多选当前组与 n-main 再成组，得到嵌套的 2 个组，三条原连线不变；随后撤销测试操作。
- `canvas-groups.check.mjs` 覆盖相对坐标、成组/嵌套解组世界坐标不变、父子移动去重、边框补偿、组内连线复制、级联移除及策划文档保留。原 `check.mjs` 和资产规则检查继续通过。
- 测试组均已清理；最终保留原有 7 个节点、3 条连线及原位置。改动未提交。
