---
version: alpha
name: 影约 CRM · 暗房
description: 让摄影素材成为画面主体，以中性界面和少量琥珀指引操作。
colors:
  primary: "oklch(52% 0.12 55)"
  background: "oklch(97.6% 0.004 75)"
  surface: "oklch(100% 0 0)"
  text: "oklch(18% 0.008 60)"
  muted: "oklch(50% 0.01 60)"
omitted:
  - section: typography
    reason: 沿用 frontend/src/index.css 的 font-body、font-display、font-mono 及中文回退字体。
  - section: spacing
    reason: 沿用共享页面与控件样式；创意空间的网格间距仅由 creative.css 定义。
  - section: rounded
    reason: 控件圆角沿用共享 btn/input，内容容器由现有页面语义定义。
  - section: components
    reason: 共享组件及交互归属见 UX-CONTRACT.md，不复制一套组件 token。
---

# 暗房

这是现有界面的扫描记录，不引入新品牌。视觉权威是 `frontend/src/index.css` 开头的暗房约定与运行时 token；本文件解释设计意图。颜色修改首先发生在该权威文件，再同步本记录，不从此文件生成第二份 CSS。

照片是唯一的大面积高饱和内容。界面使用暖轴中性色；琥珀只服务主要动作与焦点。标题、正文和数值分别消费 `--font-display`、`--font-body`、`--font-mono`。明暗主题由 `AppShell` 管理，通过同名变量切换。

创意空间采用 `docs/prototypes/creative-workspace/v3/` 的图片优先网格：素材按等宽单元并排，正文与链接也占一个单元；现场模式把参考图作为视觉中心。避免经营仪表盘、完成度、流程步骤与营销说明挤占素材位置。

共享按钮与表单沿用 `frontend/src/index.css` 的 `.btn`、`.btn-primary`、`.input`；新页面只增加布局类，不重新定义全局品牌色。反馈、确认和键盘行为见 UX-CONTRACT.md。

桌面采用宽网格，375px 下保留双列灵感墙、单列空间列表与拍摄清单；拍摄动作保持可触达。颜色、字体的实际计算值及布局以浏览器走查为证据。

## v4 原型设计方向

2026-09-07 owner 指定自适应长宽比瀑布流、懒加载与空白卡预占位，以及液态玻璃和优雅艺术气质。该方向落在独立的 [v4 原型](docs/prototypes/creative-workspace/v4/README.md)：浅雾灰绿、宋体标题、浮动玻璃导航/筛选栏，照片完整保留比例。原型运行时 token 由该目录 studio.css 管理，映射与意图见其 DESIGN.md；只用于原型走查，不改生产 index.css 或既有「暗房」约定。

同日项目原型进一步展开为概览、参考、策划、画布预览和本地现场演示，由 project.css 消费相同 token。原型可领先正式开发，但未来能力明确标注并禁用，不以视觉占位冒充实际功能。

## v5 原型设计方向

2026-09-08 owner 指定暗色基调与液态玻璃：左侧工具/资产侧栏、中央通用画布、右侧 Agent；资产库分个人库和公共市场。进一步明确策划详情是策划节点的最大化状态，不能作为侧栏独立子页面。落点为 [v5 夜间创作台](docs/prototypes/creative-workspace/v5/README.md)，运行时 token 由其 tokens.css 统一管理，资产侧栏共享该主题。v4 默认视觉和生产 CRM 保持原有约定。

2026-09-09 资产库方向更新：v5 改为原生画布辅助侧栏，用可嵌套分组导航和实时标签检索替换 v4 集合封面与独立页面交互。支持窄栏取用、拉宽整理、批量拖入画布、节点存回个人库，以及按类型导入；具体行为见 v5 的 DESIGN 与 UX-CONTRACT。

画布节点支持独立的布局分组：框选/多选打组，拖标题移动子树，子节点使用父组相对坐标；解组不改变画布位置。它与资产库分组分别承担布局组织和素材归类。

## FND-01工程验证页

`frontend/creative-lab.html`与`src/creative-canvas/lab/`仅供Vite开发环境验证，采用v5暗色/玻璃语言，运行时样例token由lab/style.css拥有。节点/边是React Flow本地view-model，不是生产API DTO；不保存业务数据。框选/平移和父子坐标沿当前基础需求，按钮/焦点/采样反馈限该独立页面，不改旧CRM组件。正式构建不把此HTML列为入口。

## 正式创意工作台：暗色与液态玻璃

owner明确要求正式工作台遵循v5原型，不受旧CRM朴素外观限制。项目画布路由采用独立的全屏画布布局，CRM导航通过左上角返回入口连接；不显示双侧CRM导航。其他CRM页面的现有样式不因该路由改变。

唯一运行时主题为`frontend/src/creative-canvas/editor/workspace.css`：背景#141619，文字#eeeee9，辅助文字#a8adb5，鼠尾草灰绿强调#bdcec9；玻璃使用#22262cde底色、28px模糊、细描边与顶部内高光，阴影建立浮层深度。正文沿PingFang/system字体，品牌与空画布标题用Songti；内容节点13px圆角、1px选中描边。共享btn/input在该路由通过主题变体呈现，不复制交互逻辑。

画布是主体，顶部栏67px、54px通高悬浮工具轨、318px可收起资产库；文字和链接通过居中玻璃编辑窗口编辑。工具和面板不随保存提示跳动。窄屏采用覆盖式面板而非长页面堆叠；可随时收起回到画布。仅呈现已经实现的功能，不用假Agent或生成按钮填充画面。

正常操作静默保存；持久错误与恢复操作、离线和未提交正文草稿才显示提示。手势规则保持滚轮平移、Ctrl/Command滚轮缩放、空格/中键拖动画布。遵循减少动态效果设置。

## 创意空间一级首页

owner提供Buzzy首页截图作为独立产品布局参考。`/creative`现在是项目首页：独立顶部品牌导航、居中的新建项目区域、已有项目卡片及归档筛选；暗色背景和玻璃材质沿正式工作台。首页使用自然页面滚动，不把画布编辑器当空首页。项目封面是抽象图形，不伪造素材或生成成果，不复制参考站的促销、模型和未实现Agent入口。

`/creative/canvases/:canvasID`进入全屏项目画布，左上角返回创意空间首页；`/creative/library`承载无项目的个人资产库。项目创建与浏览集中在首页，编辑器侧栏专注资产。首页布局由editor/projects.css拥有，颜色与控件消费同一workbench主题；手机上保留创建项目、项目列表、资产库入口。

首页新建区域是整块可点击的玻璃按钮，项目区的「+」与新建卡片行为一致：直接创建「未命名项目」并进入空白画布。去掉前置命名输入，项目名在画布中修改。

## v5 实现对齐（2026-09-10）

owner再次明确：功能范围可以少，已实现部分的视觉和交互必须遵循v5。正式工作台的设计依据是 `docs/prototypes/creative-workspace/v5/tokens.css`、`canvas.css` 与 `asset-shelf.css`，不是旧CRM样式或早期FND-02表单布局。生产样式仍由 `editor/workspace.css` 和 `library.css` 维护；不导入原型演示数据或演示脚本。

- 顶部名称直接可改；主工具为浅灰绿“+”，文字/链接在新建菜单中选择。选择/平移、帮助位于通高工具轨。
- 内容节点采用41px标题栏、13px圆角、18px宋体文字与石墨灰绿正文区域。节点不做玻璃；玻璃仅用于工具、资产侧栏与窗口。操作浮条位于所选节点上方，随拖动与视口变化，限制在视口内。
- 资产侧栏318px，两列卡片；拖宽到500px以上显示188px目录树，展开按钮切换到720px。纯文字也用宋体预览卡片、底部名称与菜单，不退化成按钮列表。筛选采用280px以内浮层；批量操作仅选中后出现。
- 开合或拖宽资产侧栏只改变浮层，不自动fitView或改动节点位置。缩放控件在桌面随侧栏让位，1200px以下侧栏覆盖画布；760px以下保留工具轨和窄屏资产入口。用户主动点击适应视图才重新构图。
- 个人库页沿相同侧栏卡片/目录体系横向展开。项目首页沿owner后续Buzzy参考的独立入口，不退回原型直接进入项目的导航结构。

媒体、连线、布局分组和Agent仍按后续feature实现；其未完成不改变当前文字节点、资产卡片和面板的设计标准。浏览器回归同时记录实际尺寸、字体、圆角、玻璃模糊与开合视口不变，并保留桌面/手机截图。
