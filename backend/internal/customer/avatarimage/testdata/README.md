# Avatar processor fixtures

- `tux.lossless.webp`：来自 `golang.org/x/image` v0.44.0 `testdata/`（Go 项目 BSD-3-Clause）。
- `orientation_6.jpg`：来自 `github.com/disintegration/imaging` v1.6.2 `testdata/`（MIT）。

两者只用于格式解码与 EXIF 方向回归，不含项目客户或其他 PII。
