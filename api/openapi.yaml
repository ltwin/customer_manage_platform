# 摄影师私域客户经营系统（CRM）HTTP API 契约。
# 语义权威源：.codestable/roadmap/photographer-private-crm/photographer-private-crm-roadmap.md §4；
# 本文件是其机器可执行形式（忠实转写，不做再设计）。发现契约问题回 cs-roadmap update，不在此私改。
# 端点集合 = §4.1 platform（login/me）+ §4.3 各域清单 + §4.6 export；GET /me 已于 2026-07-10 收编并增加 timezone。
# /healthz 为运维端点，不进本契约。
openapi: 3.0.3
info:
  title: Photographer Private CRM API
  version: 0.1.0
  description: 首版单账号；所有资源端点隐式按当前账号过滤，客户端永不传 account_id（ADR-001）。

servers:
  - url: /api/v1

security:
  - bearerAuth: []

tags:
  - name: auth
    description: 登录与当前账号（platform-skeleton 已实现）
  - name: customer-core
    description: 后端 codegen 切片标签（customer-core 已实现的客户三操作）
  - name: customer-profile-complete
    description: 后端 codegen 切片标签（customer-profile-complete 的档案五操作）
  - name: customer-avatar
    description: 后端 codegen 切片标签（客户头像条件写与鉴权读取）
  - name: reminder-engine
    description: 后端 codegen 切片标签（reminder-engine 的提醒五操作 + 设置读写）
  - name: customers
    description: 客户域（customer-core / customer-profile-complete）
  - name: packages
    description: 套系域（package-catalog）
  - name: orders
    description: 订单域（order-tracking）
  - name: schedule
    description: 档期域（schedule-slots，未实现）
  - name: reminders
    description: 提醒域（reminder-engine）
  - name: settings
    description: 设置（GET/PATCH 归 reminder-engine；telegram 绑定归 telegram-digest，未实现）
  - name: dashboard
    description: dashboard 聚合（dashboard，未实现）
  - name: export
    description: 全量导出（data-export，未实现）

paths:
  /auth/login:
    post:
      tags: [auth]
      operationId: login
      summary: 单账号密码登录，签发 Bearer token
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [password]
              properties:
                password:
                  type: string
      responses:
        "200":
          description: 登录成功
          content:
            application/json:
              schema:
                type: object
                required: [token]
                properties:
                  token:
                    type: string
        "400":
          $ref: "#/components/responses/ValidationFailed"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/Internal"

  /me:
    get:
      tags: [auth]
      operationId: getMe
      summary: 当前账号信息（永不含 password_hash）
      responses:
        "200":
          description: 当前账号
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Account"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/Internal"

  /customers:
    post:
      tags: [customers, customer-core]
      operationId: createCustomer
      summary: 30 秒建档端点（仅 3-4 项必填）
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [display_name, channel, identities]
              properties:
                display_name:
                  type: string
                channel:
                  $ref: "#/components/schemas/CustomerChannel"
                referrer_customer_id:
                  type: string
                  description: channel=referral 时必填；介绍人须 active 且本账号可见，非 active 或跨账号按 404 处理
                identities:
                  type: array
                  minItems: 1
                  items:
                    type: object
                    required: [platform, handle]
                    properties:
                      platform:
                        $ref: "#/components/schemas/SocialPlatform"
                      handle:
                        type: string
                      remark:
                        type: string
      responses:
        "201":
          description: 已建档
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Customer"
        "400":
          $ref: "#/components/responses/ValidationFailed"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/Internal"
    get:
      tags: [customers, customer-core]
      operationId: listCustomers
      summary: 客户列表（q 匹配 display_name/real_name/phone/identity.handle）
      parameters:
        - name: q
          in: query
          schema:
            type: string
        - name: channel
          in: query
          schema:
            $ref: "#/components/schemas/CustomerChannel"
        - name: status
          in: query
          description: 缺省 active；兼容单值/all，也接受逗号分隔的 active/archived/merged 状态集合；重复值、未知值或 all 与其他值混用均为 400；过滤发生在分页前
          schema:
            type: string
            pattern: "^(all|(active|merged|archived)(,(active|merged|archived))*)$"
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/PageSize"
      responses:
        "200":
          description: 分页客户列表
          content:
            application/json:
              schema:
                type: object
                required: [items, total]
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/CustomerListItem"
                  total:
                    type: integer
        "400":
          $ref: "#/components/responses/ValidationFailed"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/Internal"

  /customers/{id}:
    get:
      tags: [customers, customer-core]
      operationId: getCustomer
      summary: 客户详情（含 identities、notes 倒序、referrer 摘要）
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200":
          description: 客户详情
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerDetail"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/Internal"
    patch:
      tags: [customers, customer-profile-complete]
      operationId: updateCustomer
      summary: 渐进补全任意字段；{status:archived} 即归档（§4.2）
      parameters:
        - $ref: "#/components/parameters/Id"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                display_name:
                  type: string
                  description: 不可清空（不接受 null）
                real_name:
                  type: string
                  nullable: true
                  description: 显式传 null 清空
                phone:
                  type: string
                  nullable: true
                  description: 显式传 null 清空
                birthday:
                  nullable: true
                  allOf:
                    - $ref: "#/components/schemas/Birthday"
                  description: 显式传 null 清空
                channel:
                  $ref: "#/components/schemas/CustomerChannel"
                referrer_customer_id:
                  type: string
                  description: channel=referral 时必填；介绍人须 active 且本账号可见，非 active 或跨账号按 404 处理；不得为本客户自身（400）
                status:
                  type: string
                  enum: [active, archived]
                  description: merged 是 merge 端点专属终态，不可经 PATCH 设置
      responses:
        "200":
          description: 更新后的客户
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Customer"
        "400":
          $ref: "#/components/responses/ValidationFailed"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: customer_merged（merged 客户档案只读）
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "500":
          $ref: "#/components/responses/Internal"

  /customers/{id}/avatar:
    put:
      tags: [customers, customer-avatar]
      operationId: putCustomerAvatar
      summary: 条件设置或替换客户头像；active/archived 可写，merged 恒为 customer_merged
      parameters:
        - $ref: "#/components/parameters/Id"
        - $ref: "#/components/parameters/AvatarIfMatch"
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
      responses:
        "200":
          description: 设置后的客户；same-content 完整对象重放为 no-op
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Customer"
        "400":
          $ref: "#/components/responses/ValidationFailed"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: customer_merged | avatar_revision_conflict
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "500":
          $ref: "#/components/responses/Internal"
    delete:
      tags: [customers, customer-avatar]
      operationId: deleteCustomerAvatar
      summary: 条件移除客户头像；merged 仅开放本 cleanup-only 操作
      parameters:
        - $ref: "#/components/parameters/Id"
        - $ref: "#/components/parameters/AvatarIfMatch"
      responses:
        "200":
          description: 移除后的客户；已无头像时为 no-op
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Customer"
        "400":
          $ref: "#/components/responses/ValidationFailed"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: avatar_revision_conflict
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "500":
          $ref: "#/components/responses/Internal"

  /customers/{id}/avatar/content:
    get:
      tags: [customers, customer-avatar]
      operationId: getCustomerAvatarContent
      summary: 经账号鉴权读取当前强版本头像内容
      parameters:
        - $ref: "#/components/parameters/Id"
        - name: v
          in: query
          required: true
          schema:
            $ref: "#/components/schemas/AvatarVersion"
        - name: If-None-Match
          in: header
          required: false
          schema:
            type: string
            pattern: '^"sha256-[0-9a-f]{64}"$'
      responses:
        "200":
          description: 完整性已验证的当前头像字节
          headers:
            ETag:
              schema:
                type: string
            Cache-Control:
              schema:
                type: string
            Vary:
              schema:
                type: string
            X-Content-Type-Options:
              schema:
                type: string
          content:
            image/jpeg:
              schema:
                type: string
                format: binary
            image/png:
              schema:
                type: string
                format: binary
            image/webp:
              schema:
                type: string
                format: binary
        "304":
          description: 完整性验证通过且 If-None-Match 命中
          headers:
            ETag:
              schema:
                type: string
            Cache-Control:
              schema:
                type: string
            Vary:
              schema:
                type: string
            X-Content-Type-Options:
              schema:
                type: string
        "400":
          $ref: "#/components/responses/ValidationFailed"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: avatar_version_stale
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "500":
          $ref: "#/components/responses/Internal"
  /customers/{id}/identities:
    post:
      tags: [customers, customer-profile-complete]
      operationId: addCustomerIdentity
      summary: 追加社交身份
      parameters:
        - $ref: "#/components/parameters/Id"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [platform, handle]
              properties:
                platform:
                  $ref: "#/components/schemas/SocialPlatform"
                handle:
                  type: string
                remark:
                  type: string
      responses:
        "201":
          description: 已追加
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SocialIdentity"
        "400":
          $ref: "#/components/responses/ValidationFailed"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: customer_merged（merged 客户档案只读）
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "500":
          $ref: "#/components/responses/Internal"

  /customers/{id}/identities/{identity_id}:
    delete:
      tags: [customers, customer-profile-complete]
      operationId: deleteCustomerIdentity
      summary: 删除社交身份
      parameters:
        - $ref: "#/components/parameters/Id"
        - name: identity_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: 已删除
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: customer_merged（merged 客户档案只读）| last_identity（非 merged 客户至少保留 1 个社交身份）
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "500":
          $ref: "#/components/responses/Internal"

  /customers/{id}/notes:
    post:
      tags: [customers, customer-profile-complete]
      operationId: addCustomerNote
      summary: 追加客户备注
      parameters:
        - $ref: "#/components/parameters/Id"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [content]
              properties:
                content:
                  type: string
                  maxLength: 500
      responses:
        "201":
          description: 已追加
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerNote"
        "400":
          $ref: "#/components/responses/ValidationFailed"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: customer_merged（merged 客户档案只读）
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "500":
          $ref: "#/components/responses/Internal"

  /customers/{id}/merge:
    post:
      tags: [customers, customer-profile-complete]
      operationId: mergeCustomer
      summary: 把 source 客户 merge 进当前客户（语义见 §4.2）
      parameters:
        - $ref: "#/components/parameters/Id"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [source_customer_id]
              properties:
                source_customer_id:
                  type: string
      responses:
        "200":
          description: merge 后的 target 客户
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Customer"
        "400":
          $ref: "#/components/responses/ValidationFailed"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: merge_conflict（source 或 target 非 active）
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "500":
          $ref: "#/components/responses/Internal"

  /packages:
    post:
      tags: [packages]
      operationId: createPackage
      summary: 新建套系
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PackageInput"
      responses:
        "201":
          description: 已创建
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Package"
        "400":
          $ref: "#/components/responses/ValidationFailed"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/Internal"
    get:
      tags: [packages]
      operationId: listPackages
      summary: 套系列表（?status=active 供下单选择）
      parameters:
        - name: status
          in: query
          description: 缺省 active；可显式查 archived/all（§4.3 套系上架/下架语义）
          schema:
            type: string
            enum: [active, archived, all]
            x-enum-varnames:
              - ListPackagesParamsStatusActive
              - ListPackagesParamsStatusArchived
              - ListPackagesParamsStatusAll
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/PageSize"
      responses:
        "200":
          description: 分页套系列表
          content:
            application/json:
              schema:
                type: object
                required: [items, total]
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/PackageListItem"
                  total:
                    type: integer
        "400":
          $ref: "#/components/responses/ValidationFailed"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/Internal"

  /packages/{id}:
    patch:
      tags: [packages]
      operationId: updatePackage
      summary: 更新套系；归档 = PATCH {status:archived}（无 in-use 校验，§4.3）
      parameters:
        - $ref: "#/components/parameters/Id"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                shoot_type:
                  $ref: "#/components/schemas/ShootType"
                pricing_mode:
                  $ref: "#/components/schemas/PricingMode"
                base_price:
                  type: integer
                  description: 分
                duration_minutes:
                  type: integer
                  nullable: true
                shot_count_min:
                  type: integer
                  nullable: true
                shot_count_max:
                  type: integer
                  nullable: true
                raw_delivery_count:
                  type: integer
                  nullable: true
                retouch_count:
                  type: integer
                  nullable: true
                  description: 0=不含精修
                note:
                  type: string
                  maxLength: 500
                status:
                  $ref: "#/components/schemas/PackageStatus"
      responses:
        "200":
          description: 更新后的套系
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Package"
        "400":
          $ref: "#/components/responses/ValidationFailed"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/Internal"
    delete:
      tags: [packages]
      operationId: deletePackage
      summary: 删除套系；被订单引用时返回 package_in_use（§4.3）
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "204":
          description: 已删除，无响应体
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: package_in_use（套系已被订单引用，不可删除）
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "500":
          $ref: "#/components/responses/Internal"

  /orders:
    post:
      tags: [orders]
      operationId: createOrder
      summary: 新建订单（creation_mode=new 为新业务，backfill 为历史补录）
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [customer_id]
              properties:
                creation_mode:
                  allOf:
                    - $ref: "#/components/schemas/OrderCreationMode"
                  default: new
                  description: new=新业务，只允许 consulting/scheduled 且只能引用 active 客户/套系；backfill=历史补录，可按状态不变量直达并允许 active/archived 客户与套系，merged 客户仍拒绝
                customer_id:
                  type: string
                package_id:
                  type: string
                title:
                  type: string
                price:
                  type: integer
                  description: 分
                status:
                  allOf:
                    - $ref: "#/components/schemas/OrderStatus"
                  description: creation_mode=new 时缺省 consulting 且仅允许 consulting/scheduled；backfill 时可直达八态，≥shot 须显式 shot_at、≥delivered（含 closed）须显式 delivered_at，closed 须 balance_paid=true
                deposit_paid:
                  type: boolean
                  default: false
                balance_paid:
                  type: boolean
                  default: false
                shot_at:
                  type: string
                  format: date-time
                  description: 补录用；仅目标状态已到达 shot 时可给
                delivered_at:
                  type: string
                  format: date-time
                  description: 补录用；仅目标状态已到达 delivered 时可给
                note:
                  type: string
      responses:
        "201":
          description: 已创建
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Order"
        "400":
          $ref: "#/components/responses/ValidationFailed"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: customer_archived（new 引用 merged/archived 客户，或 backfill 引用 merged 客户）| unpaid_balance（补录 status=closed 且 balance_paid≠true）| idempotency_conflict（已成功绑定的同 key 使用不同请求体）
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "500":
          $ref: "#/components/responses/Internal"
    get:
      tags: [orders]
      operationId: listOrders
      summary: 订单列表（默认排序 created_at DESC、同值 id DESC，保证分页稳定，§4.3）
      parameters:
        - name: customer_id
          in: query
          schema:
            type: string
        - name: status
          in: query
          schema:
            $ref: "#/components/schemas/OrderStatus"
        - name: unpaid_balance
          in: query
          description: true = balance_paid=false 且 status ∈ {shot, selected, retouching, delivered}（已进入交付链条且未结清，§4.3 2026-07-09 口径）
          schema:
            type: boolean
        - name: schedulable_at
          in: query
          description: 目标 slot 的 end_at；服务端按它相对当前时刻应用与 POST/PATCH 相同的订单/客户未来历史矩阵并排除已有 shoot slot：未来要求客户 active，历史允许 active/archived，merged 永不允许；可与 customer_id 组合
          schema:
            type: string
            format: date-time
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/PageSize"
      responses:
        "200":
          description: 分页订单列表
          content:
            application/json:
              schema:
                type: object
                required: [items, total]
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/OrderListItem"
                  total:
                    type: integer
        "400":
          $ref: "#/components/responses/ValidationFailed"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/Internal"

  /orders/{id}:
    patch:
      tags: [orders]
      operationId: updateOrder
      summary: 状态跃迁与字段修正（状态机语义见 §4.2；status 等于当前状态时为 200 no-op）
      parameters:
        - $ref: "#/components/parameters/Id"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                status:
                  $ref: "#/components/schemas/OrderStatus"
                deposit_paid:
                  type: boolean
                balance_paid:
                  type: boolean
                shot_at:
                  type: string
                  format: date-time
                delivered_at:
                  type: string
                  format: date-time
                title:
                  type: string
                price:
                  type: integer
                  description: 分
                note:
                  type: string
      responses:
        "200":
          description: 更新后的订单
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Order"
        "400":
          $ref: "#/components/responses/ValidationFailed"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: invalid_status_transition（非法跃迁）| unpaid_balance（未结清进 closed / closed 订单试图取消结清标记，字段修正不变量见 §4.2）
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "500":
          $ref: "#/components/responses/Internal"
    delete:
      tags: [orders]
      operationId: deleteOrder
      summary: 物理删除终态订单（closed/cancelled，§4.3 2026-07-09）
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "204":
          description: 已删除，无响应体；实时聚合/统计即时反映（删 closed 单会减少 orders_count 与营收类统计）
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: order_not_terminal（非终态订单不可删除，先 cancel）| order_in_use（被 type=shoot 的 slot 引用；details 必返 schedule_slot_id/schedule_start_at）
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "500":
          $ref: "#/components/responses/Internal"

  /schedule/slots:
    get:
      tags: [schedule]
      operationId: listScheduleSlots
      summary: 档期区间查询（含跨界 slot）
      parameters:
        - name: from
          in: query
          required: true
          schema:
            type: string
            format: date-time
          description: 半开区间 [from,to) 的 UTC 起点；月历传完整 6 周可见网格的账号本地日界
        - name: to
          in: query
          required: true
          schema:
            type: string
            format: date-time
          description: 半开区间 [from,to) 的 UTC 终点
      responses:
        "200":
          description: 区间内档期
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ScheduleSlotListItem"
        "400":
          $ref: "#/components/responses/ValidationFailed"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/Internal"
    post:
      tags: [schedule]
      operationId: createScheduleSlot
      summary: 新建档期（支持跨日；重叠不阻止；同一订单最多一条 shoot slot）
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [start_at, end_at, type]
              properties:
                start_at:
                  type: string
                  format: date-time
                end_at:
                  type: string
                  format: date-time
                type:
                  $ref: "#/components/schemas/SlotType"
                order_id:
                  type: string
                  description: type=shoot 时必填；未来/进行中只允许 consulting/scheduled 且客户须 active；历史补录允许 scheduled/shot/selected/retouching/delivered/closed 且客户可 active/archived；cancelled 或 merged 客户永不允许
                note:
                  type: string
                  maxLength: 500
      responses:
        "201":
          description: 已创建（含重叠提示）
          content:
            application/json:
              schema:
                type: object
                required: [slot, overlaps]
                properties:
                  slot:
                    $ref: "#/components/schemas/ScheduleSlot"
                  overlaps:
                    type: array
                    items:
                      type: string
                    description: 与新 slot 重叠的 slot_id 列表
        "400":
          $ref: "#/components/responses/ValidationFailed"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: order_already_scheduled（订单已有 shoot slot，details 返回现有 slot）| customer_archived（未来档期的客户已归档/合并）| customer_changed（并发 merge 导致订单客户连续变化，需重拉候选）| idempotency_conflict（已成功绑定的同 key 使用不同请求体）
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "500":
          $ref: "#/components/responses/Internal"

  /schedule/slots/{id}:
    patch:
      tags: [schedule]
      operationId: updateScheduleSlot
      summary: 更新档期；时间/type/order 变化重验订单与客户矩阵，note-only 不重验外部状态
      parameters:
        - $ref: "#/components/parameters/Id"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                start_at:
                  type: string
                  format: date-time
                end_at:
                  type: string
                  format: date-time
                type:
                  $ref: "#/components/schemas/SlotType"
                order_id:
                  type: string
                  nullable: true
                  description: 显式 null 清空关联；省略表示不改
                note:
                  type: string
                  nullable: true
                  maxLength: 500
                  description: 显式 null 清空备注；省略表示不改
      responses:
        "200":
          description: 更新后的档期
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScheduleSlot"
        "400":
          $ref: "#/components/responses/ValidationFailed"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: order_already_scheduled（目标订单已有另一条 shoot slot，details 返回现有 slot）| customer_archived（未来档期的客户已归档/合并）| customer_changed（并发 merge 导致订单客户连续变化，需重拉候选）
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "500":
          $ref: "#/components/responses/Internal"
    delete:
      tags: [schedule]
      operationId: deleteScheduleSlot
      summary: 删除档期（不自动变更订单状态，§4.2）
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "204":
          description: 已删除
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/Internal"

  /reminders:
    get:
      tags: [reminders, reminder-engine]
      operationId: listReminders
      summary: 提醒列表
      description: 默认按 due_date ASC, id ASC 稳定排序
      parameters:
        - name: status
          in: query
          schema:
            $ref: "#/components/schemas/ReminderStatus"
        - name: customer_id
          in: query
          schema:
            type: string
        - name: due_before
          in: query
          schema:
            type: string
            format: date
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/PageSize"
      responses:
        "200":
          description: 分页提醒列表
          content:
            application/json:
              schema:
                type: object
                required: [items, total]
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/Reminder"
                  total:
                    type: integer
        "400":
          $ref: "#/components/responses/ValidationFailed"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/Internal"
    post:
      tags: [reminders, reminder-engine]
      operationId: createReminder
      summary: 手工创建自定义提醒
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [type, due_date, content]
              properties:
                type:
                  type: string
                  enum: [custom]
                customer_id:
                  type: string
                due_date:
                  type: string
                  format: date
                content:
                  type: string
      responses:
        "201":
          description: 已创建
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Reminder"
        "400":
          $ref: "#/components/responses/ValidationFailed"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/Internal"

  /reminders/{id}/done:
    post:
      tags: [reminders, reminder-engine]
      operationId: markReminderDone
      summary: 标记提醒完成
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200":
          description: 更新后的提醒
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Reminder"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/Internal"

  /reminders/{id}/dismiss:
    post:
      tags: [reminders, reminder-engine]
      operationId: dismissReminder
      summary: 忽略提醒
      parameters:
        - $ref: "#/components/parameters/Id"
      responses:
        "200":
          description: 更新后的提醒
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Reminder"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/Internal"

  /admin/reminders/scan:
    post:
      tags: [reminders, reminder-engine]
      operationId: scanReminders
      summary: 提醒扫描唯一手动触发入口（每日定时与手动共用，鉴权同 §4.1）
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                date:
                  type: string
                  format: date
                  description: 缺省=账号时区今日
      responses:
        "200":
          description: 扫描结果计数
          content:
            application/json:
              schema:
                type: object
                required: [created, skipped, auto_dismissed]
                properties:
                  created:
                    type: integer
                  skipped:
                    type: integer
                  auto_dismissed:
                    type: integer
        "400":
          $ref: "#/components/responses/ValidationFailed"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/Internal"

  /settings:
    get:
      tags: [settings, reminder-engine]
      operationId: getSettings
      summary: 读取设置
      responses:
        "200":
          description: 当前设置
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Settings"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/Internal"
    patch:
      tags: [settings, reminder-engine]
      operationId: updateSettings
      summary: 更新设置
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                timezone:
                  type: string
                  description: IANA 时区
                birthday_lead_days:
                  type: integer
                follow_up_after_days:
                  type: integer
                churn_thresholds:
                  type: array
                  items:
                    $ref: "#/components/schemas/ChurnThreshold"
                digest_hour:
                  type: integer
                  minimum: 0
                  maximum: 23
      responses:
        "200":
          description: 更新后的设置
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Settings"
        "400":
          $ref: "#/components/responses/ValidationFailed"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/Internal"

  /settings/telegram/bind-token:
    post:
      tags: [settings]
      operationId: createTelegramBindToken
      summary: 生成 TG 绑定 token 与 deep link（绑定流程见 §4.5）
      responses:
        "200":
          description: 绑定 token（10min 有效）
          content:
            application/json:
              schema:
                type: object
                required: [token, deep_link]
                properties:
                  token:
                    type: string
                  deep_link:
                    type: string
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/Internal"

  /dashboard:
    get:
      tags: [dashboard]
      operationId: getDashboard
      summary: 今日经营台聚合（口径单点在服务端，§4.3）
      responses:
        "200":
          description: 聚合数据
          content:
            application/json:
              schema:
                type: object
                required:
                  [due_reminders, today_slots, unpaid_orders, churn_alerts, recent_stats]
                properties:
                  due_reminders:
                    description: due_date ≤ 账号时区今日+2 天且 pending（近 3 天窗口含逾期）
                    type: array
                    items:
                      $ref: "#/components/schemas/Reminder"
                  today_slots:
                    description: 今日档期（含订单+客户摘要，字段由 dashboard-today feature 细化）
                    type: array
                    items:
                      $ref: "#/components/schemas/ScheduleSlot"
                  unpaid_orders:
                    type: object
                    required: [count, items]
                    properties:
                      count:
                        type: integer
                      items:
                        description: status=delivered 且 balance_paid=false（closed 必已结清故不出现）
                        type: array
                        items:
                          $ref: "#/components/schemas/Order"
                  churn_alerts:
                    description: type=churn 且 pending
                    type: array
                    items:
                      $ref: "#/components/schemas/Reminder"
                  recent_stats:
                    type: object
                    required: [orders_created, orders_delivered, revenue_confirmed]
                    properties:
                      orders_created:
                        description: 近 30 天内按 created_at 统计，含全部状态
                        type: integer
                      orders_delivered:
                        description: 近 30 天内按 delivered_at 统计，排除当前 status=cancelled
                        type: integer
                      revenue_confirmed:
                        type: integer
                        description: 分；近 30 天 delivered_at 落窗口、balance_paid=true 且当前非 cancelled 的订单 price 之和
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/Internal"

  /export:
    get:
      tags: [export]
      operationId: exportAll
      summary: 全量导出（application/json 附件；counts 必须与各数组长度一致，§4.6）
      responses:
        "200":
          description: 全量导出文件（Content-Disposition 附件）
          content:
            application/json:
              schema:
                type: object
                required:
                  [exported_at, schema_version, counts, customers, social_identities,
                   customer_notes, packages, orders, schedule_slots, reminders, settings]
                properties:
                  exported_at:
                    type: string
                    format: date-time
                  schema_version:
                    type: integer
                    enum: [1]
                  counts:
                    type: object
                    required:
                      [customers, social_identities, customer_notes, packages, orders,
                       schedule_slots, reminders]
                    properties:
                      customers:
                        type: integer
                      social_identities:
                        type: integer
                      customer_notes:
                        type: integer
                      packages:
                        type: integer
                      orders:
                        type: integer
                      schedule_slots:
                        type: integer
                      reminders:
                        type: integer
                  customers:
                    type: array
                    items:
                      $ref: "#/components/schemas/Customer"
                  social_identities:
                    type: array
                    items:
                      $ref: "#/components/schemas/SocialIdentity"
                  customer_notes:
                    type: array
                    items:
                      $ref: "#/components/schemas/CustomerNote"
                  packages:
                    type: array
                    items:
                      $ref: "#/components/schemas/Package"
                  orders:
                    type: array
                    items:
                      $ref: "#/components/schemas/Order"
                  schedule_slots:
                    type: array
                    items:
                      $ref: "#/components/schemas/ScheduleSlot"
                  reminders:
                    type: array
                    items:
                      $ref: "#/components/schemas/Reminder"
                  settings:
                    $ref: "#/components/schemas/Settings"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/Internal"

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  parameters:
    AvatarIfMatch:
      name: If-Match
      in: header
      required: true
      description: 当前 Customer.avatar_revision 的 quoted token，例如 "ar-3"
      schema:
        type: string
        pattern: '^"ar-(0|[1-9][0-9]*)"$'
    IdempotencyKey:
      name: Idempotency-Key
      in: header
      required: false
      description: 可选安全重放键；组合流程及从档期跳转的历史订单补录必须传。只持久化成功 2xx；24 小时内同账号、同操作、同 key、同规范化请求返回首次成功结果；成功绑定后的同 key 异请求返回 409 idempotency_conflict；客户端收到任意 5xx 时必须用原 body/key 重放确认，不得换 key
      schema:
        type: string
        minLength: 8
        maxLength: 128
        pattern: "^[A-Za-z0-9._:-]+$"
    Id:
      name: id
      in: path
      required: true
      schema:
        type: string
    Page:
      name: page
      in: query
      schema:
        type: integer
        default: 1
        minimum: 1
    PageSize:
      name: page_size
      in: query
      schema:
        type: integer
        default: 20
        minimum: 1

  responses:
    ValidationFailed:
      description: 400 validation_failed
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
    Unauthorized:
      description: 401 unauthorized
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
    NotFound:
      description: 404 not_found
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
    Internal:
      description: 500 internal
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"

  schemas:
    ErrorEnvelope:
      description: 统一错误封套（§4.1）；错误码 validation_failed | unauthorized | not_found | conflict 子码 | internal
      type: object
      required: [error]
      properties:
        error:
          type: object
          required: [code, message]
          properties:
            code:
              type: string
            message:
              type: string
            details:
              $ref: "#/components/schemas/ScheduleConflictDetails"

    ScheduleConflictDetails:
      description: order_in_use / order_already_scheduled 的可行动上下文；出现时两个字段必返
      type: object
      required: [schedule_slot_id, schedule_start_at]
      properties:
        schedule_slot_id:
          type: string
        schedule_start_at:
          type: string
          format: date-time

    Account:
      description: 账号（摄影师）；永不含 password_hash
      type: object
      required: [id, created_at, timezone]
      properties:
        id:
          type: string
          readOnly: true
        created_at:
          type: string
          format: date-time
          readOnly: true
        timezone:
          type: string
          description: IANA 时区；Settings 未落地前返回 Asia/Shanghai，日历不得使用浏览器时区替代

    Birthday:
      description: 生日特例："MM-DD" 或 "YYYY-MM-DD"（年份可缺，§4.1）
      type: string
      pattern: "^([0-9]{4}-)?[0-9]{2}-[0-9]{2}$"

    CustomerChannel:
      type: string
      enum: [xiaohongshu, douyin, weibo, referral, other]
      x-enum-varnames:
        - CustomerChannelXiaohongshu
        - CustomerChannelDouyin
        - CustomerChannelWeibo
        - CustomerChannelReferral
        - CustomerChannelOther

    CustomerStatus:
      type: string
      enum: [active, merged, archived]

    AvatarRevision:
      type: string
      readOnly: true
      pattern: "^ar-(0|[1-9][0-9]*)$"
      example: ar-0

    AvatarVersion:
      type: string
      readOnly: true
      pattern: "^sha256-[0-9a-f]{64}$"

    SocialPlatform:
      type: string
      enum: [wechat, qq, telegram, xiaohongshu, douyin, weibo, other]
      x-enum-varnames:
        - SocialPlatformWechat
        - SocialPlatformQq
        - SocialPlatformTelegram
        - SocialPlatformXiaohongshu
        - SocialPlatformDouyin
        - SocialPlatformWeibo
        - SocialPlatformOther

    ShootType:
      type: string
      enum: [portrait, cosplay, other]
      x-enum-varnames:
        - ShootTypePortrait
        - ShootTypeCosplay
        - ShootTypeOther

    PricingMode:
      type: string
      enum: [per_duration, per_photo, fixed]
      x-enum-varnames:
        - PricingModePerDuration
        - PricingModePerPhoto
        - PricingModeFixed

    PackageStatus:
      type: string
      enum: [active, archived]
      x-enum-varnames:
        - PackageStatusActive
        - PackageStatusArchived

    OrderStatus:
      description: 语义与合法跃迁见 §4.2 订单状态机
      type: string
      enum: [consulting, scheduled, shot, selected, retouching, delivered, closed, cancelled]

    OrderCreationMode:
      type: string
      enum: [new, backfill]

    SlotType:
      type: string
      enum: [shoot, hold, busy]

    ReminderType:
      type: string
      enum: [birthday, follow_up, churn, custom]

    ReminderStatus:
      type: string
      enum: [pending, done, dismissed]

    Customer:
      type: object
      required: [id, account_id, created_at, display_name, channel, status, avatar_revision]
      properties:
        id:
          type: string
          readOnly: true
        account_id:
          type: string
          readOnly: true
          description: 服务端由账号上下文写入，客户端永不传（ADR-001）
        created_at:
          type: string
          format: date-time
          readOnly: true
        display_name:
          type: string
        real_name:
          type: string
        phone:
          type: string
        birthday:
          $ref: "#/components/schemas/Birthday"
        channel:
          $ref: "#/components/schemas/CustomerChannel"
        referrer_customer_id:
          type: string
          description: channel=referral 时必填
        status:
          $ref: "#/components/schemas/CustomerStatus"
        merged_into_customer_id:
          type: string
          readOnly: true
        avatar_revision:
          $ref: "#/components/schemas/AvatarRevision"
        avatar_version:
          $ref: "#/components/schemas/AvatarVersion"
        avatar_url:
          type: string
          readOnly: true
          description: 仅当前有头像时存在；同源鉴权相对 URL，v 等于 avatar_version

    CustomerListItem:
      allOf:
        - $ref: "#/components/schemas/Customer"
        - type: object
          required: [orders_count, last_shot_at]
          properties:
            orders_count:
              type: integer
              description: 非 cancelled 订单计数；order 域未落地前恒为 0
            last_shot_at:
              type: string
              format: date
              nullable: true
              description: 非 cancelled 订单 max(shot_at) 按账号时区截断；order 域未落地前恒为 null

    CustomerSummary:
      type: object
      required: [id, display_name, channel, status, avatar_revision]
      properties:
        id:
          type: string
        display_name:
          type: string
        channel:
          $ref: "#/components/schemas/CustomerChannel"
        status:
          $ref: "#/components/schemas/CustomerStatus"
        avatar_revision:
          $ref: "#/components/schemas/AvatarRevision"
        avatar_version:
          $ref: "#/components/schemas/AvatarVersion"
        avatar_url:
          type: string
          readOnly: true

    CustomerStats:
      type: object
      required: [orders_count, total_order_amount, last_shot_at]
      properties:
        orders_count:
          type: integer
          description: 非 cancelled 订单计数；order 域未落地前恒为 0
        total_order_amount:
          type: integer
          description: 分；非 cancelled 订单 price 之和；order 域未落地前恒为 0
        last_shot_at:
          type: string
          format: date
          nullable: true
          description: 口径同 CustomerListItem.last_shot_at

    CustomerDetail:
      allOf:
        - $ref: "#/components/schemas/Customer"
        - type: object
          required: [identities, notes, referrer, stats]
          properties:
            identities:
              type: array
              items:
                $ref: "#/components/schemas/SocialIdentity"
            notes:
              description: 倒序
              type: array
              items:
                $ref: "#/components/schemas/CustomerNote"
            referrer:
              nullable: true
              allOf:
                - $ref: "#/components/schemas/CustomerSummary"
            stats:
              $ref: "#/components/schemas/CustomerStats"

    SocialIdentity:
      type: object
      required: [id, account_id, created_at, customer_id, platform, handle]
      properties:
        id:
          type: string
          readOnly: true
        account_id:
          type: string
          readOnly: true
          description: 服务端由账号上下文写入，客户端永不传（ADR-001）
        created_at:
          type: string
          format: date-time
          readOnly: true
        customer_id:
          type: string
        platform:
          $ref: "#/components/schemas/SocialPlatform"
        handle:
          type: string
        remark:
          type: string

    CustomerNote:
      type: object
      required: [id, account_id, created_at, customer_id, content]
      properties:
        id:
          type: string
          readOnly: true
        account_id:
          type: string
          readOnly: true
          description: 服务端由账号上下文写入，客户端永不传（ADR-001）
        created_at:
          type: string
          format: date-time
          readOnly: true
        customer_id:
          type: string
        content:
          type: string
          maxLength: 500

    PackageInput:
      type: object
      required: [name, shoot_type, pricing_mode, base_price]
      properties:
        name:
          type: string
        shoot_type:
          $ref: "#/components/schemas/ShootType"
        pricing_mode:
          $ref: "#/components/schemas/PricingMode"
        base_price:
          type: integer
          description: 分
        duration_minutes:
          type: integer
        shot_count_min:
          type: integer
        shot_count_max:
          type: integer
        raw_delivery_count:
          type: integer
        retouch_count:
          type: integer
          description: 0=不含精修
        note:
          type: string
          maxLength: 500

    Package:
      allOf:
        - $ref: "#/components/schemas/PackageInput"
        - type: object
          required: [id, account_id, created_at, status]
          properties:
            id:
              type: string
              readOnly: true
            account_id:
              type: string
              readOnly: true
              description: 服务端由账号上下文写入，客户端永不传（ADR-001）
            created_at:
              type: string
              format: date-time
              readOnly: true
            status:
              $ref: "#/components/schemas/PackageStatus"

    PackageListItem:
      allOf:
        - $ref: "#/components/schemas/Package"
        - type: object
          required: [orders_count]
          properties:
            orders_count:
              type: integer
              description: 引用本套系的非 cancelled 订单计数；order 域未落地前恒为 0

    Order:
      type: object
      required: [id, account_id, created_at, customer_id, status, deposit_paid, balance_paid]
      properties:
        id:
          type: string
          readOnly: true
        account_id:
          type: string
          readOnly: true
          description: 服务端由账号上下文写入，客户端永不传（ADR-001）
        created_at:
          type: string
          format: date-time
          readOnly: true
        customer_id:
          type: string
        package_id:
          type: string
        title:
          type: string
        status:
          $ref: "#/components/schemas/OrderStatus"
        price:
          type: integer
          description: 分
        deposit_paid:
          type: boolean
          default: false
        balance_paid:
          type: boolean
          default: false
        shot_at:
          type: string
          format: date-time
          description: 进入 shot 时服务端自动写入，事后可 PATCH 修正（§4.2）
        delivered_at:
          type: string
          format: date-time
          description: 进入 delivered 时服务端自动写入，事后可 PATCH 修正（§4.2）
        note:
          type: string

    OrderListItem:
      allOf:
        - $ref: "#/components/schemas/Order"
        - type: object
          required: [customer_display_name]
          properties:
            customer_display_name:
              type: string
              description: 引用客户的 display_name（列表可读性摘要，§4.3 2026-07-09 update）
            package_name:
              type: string
              description: 引用套系的 name；未引用套系时缺省

    ScheduleSlot:
      type: object
      required: [id, account_id, created_at, start_at, end_at, type]
      properties:
        id:
          type: string
          readOnly: true
        account_id:
          type: string
          readOnly: true
          description: 服务端由账号上下文写入，客户端永不传（ADR-001）
        created_at:
          type: string
          format: date-time
          readOnly: true
        start_at:
          type: string
          format: date-time
        end_at:
          type: string
          format: date-time
          description: 必须 > start_at
        type:
          $ref: "#/components/schemas/SlotType"
        order_id:
          type: string
          description: type=shoot 时必填；同一订单最多关联一条 shoot slot
        note:
          type: string
          maxLength: 500

    ScheduleSlotListItemBase:
      type: object
      required: [id, account_id, created_at, start_at, end_at]
      properties:
        id:
          type: string
          readOnly: true
        account_id:
          type: string
          readOnly: true
          description: 服务端由账号上下文写入，客户端永不传（ADR-001）
        created_at:
          type: string
          format: date-time
          readOnly: true
        start_at:
          type: string
          format: date-time
        end_at:
          type: string
          format: date-time
          description: 必须 > start_at
        note:
          type: string
          maxLength: 500

    ShootScheduleSlotListItem:
      allOf:
        - $ref: "#/components/schemas/ScheduleSlotListItemBase"
        - type: object
          required: [type, order_id, customer_id, customer_display_name, customer_status, order_status]
          properties:
            type:
              type: string
              enum: [shoot]
            order_id:
              type: string
              description: shoot slot 必返的关联订单 id
            customer_id:
              type: string
              description: 引用订单的当前客户 id；merge 后返回 target id，供恢复流程与客户档案订单 tab 使用
            customer_display_name:
              type: string
              description: 引用订单的当前客户名
            customer_status:
              allOf:
                - $ref: "#/components/schemas/CustomerStatus"
              description: 当前客户状态；客户归档后仍返回，供文本警示
            order_title:
              type: string
              description: 引用订单的标题；无标题时缺省，由前端按套系/客户兜底
            package_name:
              type: string
              description: 引用订单所选套系名；未选套系时缺省
            order_status:
              $ref: "#/components/schemas/OrderStatus"

    NonShootScheduleSlotListItem:
      allOf:
        - $ref: "#/components/schemas/ScheduleSlotListItemBase"
        - type: object
          required: [type]
          properties:
            type:
              type: string
              enum: [hold, busy]
              description: hold/busy 不带订单或客户引用摘要

    ScheduleSlotListItem:
      oneOf:
        - $ref: "#/components/schemas/ShootScheduleSlotListItem"
        - $ref: "#/components/schemas/NonShootScheduleSlotListItem"
      discriminator:
        propertyName: type
        mapping:
          shoot: "#/components/schemas/ShootScheduleSlotListItem"
          hold: "#/components/schemas/NonShootScheduleSlotListItem"
          busy: "#/components/schemas/NonShootScheduleSlotListItem"

    Reminder:
      type: object
      required: [id, account_id, created_at, type, due_date, content, status, dedup_key]
      properties:
        id:
          type: string
          readOnly: true
        account_id:
          type: string
          readOnly: true
          description: 服务端由账号上下文写入，客户端永不传（ADR-001）
        created_at:
          type: string
          format: date-time
          readOnly: true
        type:
          $ref: "#/components/schemas/ReminderType"
        customer_id:
          type: string
        order_id:
          type: string
        due_date:
          type: string
          format: date
        content:
          type: string
        status:
          $ref: "#/components/schemas/ReminderStatus"
        dedup_key:
          type: string
          description: unique per account（规则见 §4.4）

    ChurnThreshold:
      type: object
      required: [shoot_type, days]
      properties:
        shoot_type:
          $ref: "#/components/schemas/ShootType"
        days:
          type: integer

    Settings:
      type: object
      required:
        [timezone, birthday_lead_days, follow_up_after_days, churn_thresholds, digest_hour]
      properties:
        timezone:
          type: string
          description: IANA，默认 Asia/Shanghai；所有 date-only 判定按此时区（§4.1）
        birthday_lead_days:
          type: integer
          default: 3
        follow_up_after_days:
          type: integer
          default: 7
        churn_thresholds:
          type: array
          items:
            $ref: "#/components/schemas/ChurnThreshold"
          description: 默认全类型 180
        digest_hour:
          type: integer
          minimum: 0
          maximum: 23
          default: 9
          description: 按 timezone
        telegram_chat_id:
          type: string
